{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h1>Ajouter un batiment</h1>

                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary">Enregistrer</button>
                {{ form_end(form) }}

                <ul>
                    <li>
                        <a href="{{ path('batiment_index') }}">Retourner à la liste</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

{% endblock %}
