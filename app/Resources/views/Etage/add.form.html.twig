{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Ajouter un étage</h1>

                {{ form_start(etage_form) }}
                {{ form_widget(etage_form) }}
                <button type="submit" class="btn btn-primary">Enregistrer</button>
                {{ form_end(etage_form) }}
            </div>
        </div>
    </div>

{% endblock %}